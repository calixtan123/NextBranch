import { describe,expect,it } from 'vitest'; import { bundledTopology } from './bundledTopology'; import { byId,stations } from './stations';
describe('canonical Northern stations',()=>{it('exactly covers ordered-route station IDs',()=>{const official=new Set(bundledTopology.topology.flatMap((topology)=>topology.orderedLineRoutes.flatMap((route)=>route.naptanIds)));expect(official.size).toBe(52);expect(new Set(stations.map((station)=>station.id))).toEqual(official);});it('uses captured Kentish Town, Kings Cross and Nine Elms IDs',()=>{expect(byId.get('940GZZLUKSH')?.name).toBe('Kentish Town');expect(byId.get('940GZZLUKSX')?.name).toBe("King's Cross St. Pancras");expect(byId.get('940GZZNEUGST')?.name).toBe('Nine Elms');});});
